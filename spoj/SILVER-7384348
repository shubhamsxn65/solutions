#include<stdio.h>
#include<math.h>
int main()
{
	 long int t,i,p;
	 while((scanf("%ld",&t)) && t!=0)
	 {
	 	for(i=0;i>=0;i++)
	 	{
	 		p=pow(2,i);
	 		if(p>t)
	 			break;
	 	}
	 	printf("%ld\n",i-1);
	 }
	 return 0;
}